defaults:
  - _self_

data:
  data_dir: .mega-trading/public
  labeled_prediction_path: labels/labels/labeled-predictions.jsonl

run:
  backtest_id: public-backtest

backtest:
  fee_bps: 1.0
  slippage_bps: 1.0
  confidence_threshold: 0.0
